# Store release

`#release` / skill `.cursor/skills/store-release`. TestFlight = `npm run testflight` (Xcode locale). ≠ Submit for Review.
Metadati ASC: `store/ios/` + `bash scripts/asc-metadata.sh`. Submit: `bash scripts/asc-submit.sh` solo a pacchetto completo.
Segreti: skill `secrets.md` / `~/.app-store/asc-api/`. Telefono pubblico `+393716752550`.
